<h1>Latest Posts</h1>
<% @posts.each do |post| %>
  <div>
    <%= link_to post.title, post_path(post)%>
    <%= post.user.email %>
    <%= "Edit" if post.user == @user %>
  </div>
<% end %>
<%#= render @posts %>